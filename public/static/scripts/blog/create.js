(function ($) {
  "use strict";

  var id = $("#id").val()
    , error = $("#error")
    , errorMsg = $("#errorMsg")
    , image = {};

  /**
   * 初始化
   */
  function init() {

    error.hide();

  }

  init();

}(jQuery));
